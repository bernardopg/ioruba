resolver: lts-22.41  # GHC 9.6.6

packages:
  - .

extra-deps:
  - serialport-0.6.0

ghc-options:
  "$locals": -Wall -Wcompat -Widentities -Wincomplete-record-updates -Wincomplete-uni-patterns -Wredundant-constraints

nix:
  enable: false

system-ghc: false

install-ghc: true

allow-newer: true
