# Archived GitHub Automation

This subtree keeps the old repository-surface tooling that was removed from the active workflow when the project moved to the Tauri/Rust desktop stack.

It includes:

- GitHub Pages generation scripts
- metadata sync helpers
- Release Please configuration

The active automation now lives only in:

- `.github/workflows/ci.yml`
- `.github/workflows/release.yml`
