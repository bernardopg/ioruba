# Legacy Archive

This directory keeps retired parts of the repository that are still useful for migration audits, historical context, or parity checks.

- `arduino-audio-controller`: archived Python/GTK prototype with screenshots and reference assets

Deeper migration history now lives in `docs/migration/`. The active product path lives in `apps/desktop`, `packages/shared`, and `firmware/arduino/ioruba-controller`.
